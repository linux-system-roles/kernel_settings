---
- name: Ensure that the role runs with default parameters
  hosts: all
  gather_facts: false
  roles:
    - linux-system-roles.kernel_settings
  pre_tasks:
    - fail:
        msg: var is defined
      when: my_secret_var is defined
